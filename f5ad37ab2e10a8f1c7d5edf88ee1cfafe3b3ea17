Compact causal mask cleanup in /home/gencoolpc/llama-compact-causal-mask-cleanup on branch codex/cleanup-compact-causal-mask.

Code-quality changes: resolve backend descriptor capability once per scheduler; split dense construction from compact selection; remove optional/default argument plumbing; harden compact eligibility pointer and slot checks; simplify CUDA KV_max and MMA/vector host dispatch; preserve compile-time CUDA hot-loop specialization; remove invalid/null dense-mask pointer arithmetic; make CPU descriptor access typed and explicit; simplify compact-equivalence test construction and exact op filtering; keep the compact compile gate scoped to CUDA so HIP/MUSA retain dense fallback.

Final diff against merged PR #53: 11 files, 185 insertions, 190 deletions. git diff --check and ASCII scan clean. Release CPU and CUDA builds passed with shared ccache and build locks. Changed-lines clang-tidy is clean for production changes; the remaining test_case::mode analyzer warning predates this feature. Final CUDA FLASH_ATTN_EXT plus compact descriptor matrix passed 2946/2946; all 10 compact-vs-dense cases passed. GPU lock released. No commit, push, or PR was created. The live-context worktree was not modified.
